{
  "name": "@media-center/algorithm",
  "version": "1.0.0",
  "main": "./lib/index",
  "license": "MIT",
  "types": "./src/index",
  "files": [
    "./src/"
  ],
  "scripts": {
    "build:watch": "yarn build --watch --preserveWatchOutput",
    "build": "tsc",
    "postinstall": "yarn build"
  },
  "devDependencies": {
    "@media-center/dev": "*"
  }
}
